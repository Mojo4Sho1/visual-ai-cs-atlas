# CLAUDE.md

This file is intentionally minimal. It does not duplicate repo rules.

1. Start by reading [`README.md`](README.md).
2. Then follow [`AGENTS.md`](AGENTS.md) and the public docs it references — those
   are the single source of truth for durable repo conventions.
3. Do not duplicate repo rules in Claude-specific files.
4. Do not treat `internal/` as a source of durable project instructions; it is
   private, git-ignored working space only.
